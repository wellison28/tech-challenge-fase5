name: customer-service

on:
  push:
    branches: [main]
    paths:
      - 'customer-service/**'
      - '.github/workflows/customer-service.yml'
  pull_request:
    paths:
      - 'customer-service/**'
      - '.github/workflows/customer-service.yml'
  # Permite disparar manualmente, para revalidar sem alterar código.
  workflow_dispatch:

# Todos os comandos rodam dentro da pasta do serviço: no monorepo, o checkout
# traz a raiz do repositório, e sem isto o npm não encontraria o package.json.
defaults:
  run:
    working-directory: customer-service

# Cancela execuções antigas do mesmo branch: economiza runner e dá feedback
# mais rápido no PR.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint, tipos e testes
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: revenda
          POSTGRES_PASSWORD: revenda
          POSTGRES_DB: customers
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://revenda:revenda@localhost:5432/customers?schema=public
      AUTH_MODE: dev
      JWT_DEV_SECRET: ci-only-secret
      NODE_ENV: test
      CRYPTO_MODE: local
      LOCAL_MASTER_KEY: Y2hhdmUtbWVzdHJhLWRlLUNJLWNvbS0zMi1ieXRlcw==
      CPF_BLIND_INDEX_PEPPER: pepper-de-ci-com-tamanho-suficiente

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: customer-service/package-lock.json

      - run: npm ci
      - run: npx prisma generate
      - run: npm run lint
      - run: npm run typecheck
      - run: npx prisma migrate deploy
      - run: npm test

      # Falha em vulnerabilidade alta ou crítica nas dependências deste serviço.
      - run: npm audit --audit-level=high

      - name: Publica o relatório de cobertura
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-customer-service
          path: customer-service/coverage/

  build:
    name: Build da imagem
    runs-on: ubuntu-latest
    needs: [quality]
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Build (sem push)
        uses: docker/build-push-action@v6
        with:
          context: ./customer-service
          push: false
          tags: customer-service:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
